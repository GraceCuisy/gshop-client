/**
 * 用于操作用户模块的数据的vuex模块
 */
const state={
  userInfo:{}, //登录的用户的信息对象
};
const mutations={};
const actions={};
const getters={};

 export default{
   state,
   mutations,
   actions,
   getters,
 }
